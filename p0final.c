#include <stdio.h>
int main(void)
{
  int length = 10;
  int breadth = 15;
  int area= length * breadth;
  printf("the area of rectangle with length = %i and breadth = %i is %i\n",length,breadth,area);
  return 0;
}
